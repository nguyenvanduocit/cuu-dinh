{
  "name": "cuu-dinh",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "lint": "echo 'TODO: configure oxlint'"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
