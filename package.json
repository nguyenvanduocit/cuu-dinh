{
  "name": "cuu-dinh",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "lint": "echo 'TODO: configure oxlint'"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0",
    "yaml": "2.9.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
