# Bảo Đan Lưu — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Tăng drop rate Linh Đan — nét glyph hình dòng chảy bảo đan, viên đan trôi trong dòng khí.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue elixir flow stream with pill droplets drifting along current path, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
