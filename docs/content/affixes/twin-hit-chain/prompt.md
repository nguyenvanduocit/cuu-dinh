# Song Hit Chuỗi — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Chance chain jump mỗi va chạm — nét glyph hình song hit chuỗi, hai điểm nối nhau nhảy chuỗi.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue twin hit chain double-node hop link motif with paired connection arc, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
