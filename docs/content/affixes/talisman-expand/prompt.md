# Bùa Mở Rộng — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Tăng tầm kích hoạt Bùa — sigil hình bùa với vòng bán kính mở rộng dần.

```
/imagine prompt: a single small centered gold sigil overlay symbol, centered gold expanding talisman with outward radius ring growing indicator, imperial gold (#D4AF37) dominant glow, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
