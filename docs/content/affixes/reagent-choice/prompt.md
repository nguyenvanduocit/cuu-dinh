# Tăng Chọn Đan Dược — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Thêm đan dược vào lựa chọn loot sau ván win — nét glyph hình lựa chọn đan dược, nhiều viên đan mở ra.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue reagent choice multiple options fan spread motif with several pill dots in arc, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
