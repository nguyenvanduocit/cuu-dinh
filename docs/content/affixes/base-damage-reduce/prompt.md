# Giảm Thiệt Hại Nền — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Giảm damage từ linh lực trung tính — nét glyph hình giảm thiệt hại nền, lớp nền hấp thu trung hoà.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue base damage reduction neutral absorb layer motif with buffering ground plane, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
