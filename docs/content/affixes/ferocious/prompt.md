# Hung Hãn — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Tăng crit chance — nét glyph đâm nhọn từ trung tâm ra như nanh thú hung dữ.

```
/imagine prompt: a single small rune glyph overlay symbol, aggressive cinnabar-red sharp fang-like spikes radiating from center, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
