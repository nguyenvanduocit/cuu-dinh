# Bách Lôi Tán Sát — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Linh lực split thành 3 đường — nét glyph hình bách lôi rẽ ba nhánh sét.

```
/imagine prompt: a single small rune glyph overlay symbol, aggressive cinnabar-red hundred thunder split into three diverging lightning bolt branches, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
