# Phi Tốc Giao Điểm — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Velocity và intensity khi charge cao — nét glyph hình giao điểm phi tốc, điểm hội tụ tốc độ.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue swift junction crosspoint velocity convergence motif with speed lines meeting at node, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
