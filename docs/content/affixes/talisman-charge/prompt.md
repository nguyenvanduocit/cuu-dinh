# Bùa Tích Luỹ — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Bùa tích charge qua va chạm rồi kích hoạt — sigil hình bùa với dấu tích charge từng bước.

```
/imagine prompt: a single small centered gold sigil overlay symbol, centered gold talisman charge accumulation segmented fill motif with rising charge bar, imperial gold (#D4AF37) dominant glow, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
