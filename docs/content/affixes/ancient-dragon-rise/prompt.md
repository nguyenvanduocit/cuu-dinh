# Cổ Long Khởi — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Linh khí đầu tiên mỗi ván nhận intensity cực cao — nét glyph hình long kỳ vút lên từ đáy.

```
/imagine prompt: a single small rune glyph overlay symbol, aggressive cinnabar-red ancient dragon rising coil stroke surging upward from base, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
