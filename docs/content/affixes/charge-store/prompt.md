# Tăng Trữ — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Tích charge mỗi va chạm — nét glyph hình bình tích năng lượng, thanh sạc dần đầy.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue charge accumulation battery fill motif with incremental segments stacking upward, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
