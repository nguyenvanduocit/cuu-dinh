# Liệt Hoả — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Tăng intensity Hoả lực — ngọn lửa bùng lên dữ dội, nét glyph hình lưỡi lửa vút cao.

```
/imagine prompt: a single small rune glyph overlay symbol, aggressive cinnabar-red upward-licking flame strokes, upward-licking flames and floating embers, cinnabar red (#C82127) radiant heat, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
