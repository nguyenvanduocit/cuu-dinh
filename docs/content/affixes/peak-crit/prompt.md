# Đỉnh Điểm Crit — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Crit gây thêm intensity — nét glyph hình đỉnh núi nhọn, mũi tên crit xuyên thẳng từ đỉnh.

```
/imagine prompt: a single small rune glyph overlay symbol, aggressive cinnabar-red peak mountain tip with crit strike arrow piercing through apex, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
