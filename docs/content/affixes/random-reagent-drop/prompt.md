# Rơi Ngẫu Nhiên Đan — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Tăng drop rate đan dược — nét glyph hình đan dược rơi ngẫu nhiên, viên đan rải từ trên xuống.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue random reagent rain drop scatter motif with falling pill dots from above, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
