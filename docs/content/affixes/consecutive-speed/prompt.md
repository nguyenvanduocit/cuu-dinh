# Phi Tốc Liên Tiếp — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Velocity tăng mỗi hit liên tiếp — nét glyph hình phi tốc liên tiếp, vạch tốc độ tích luỹ.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue consecutive speed accumulation motif with horizontal velocity bars stacking rightward, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
