# Sấm Sét — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../_style/style-suffix.md`.

Kim sét đánh linh khí ngẫu nhiên — nét glyph hình chớp giật gãy góc, nhánh sét phân ra hai.

```
/imagine prompt: a single small rune glyph overlay symbol, aggressive cinnabar-red lightning fork zigzag stroke with split branch, forged metal with a sharp angular silhouette and cold metallic gleam, imperial gold (#D4AF37) edge light, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
