# Bùa Một Lần — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Bùa kích hoạt hiệu ứng một lần mỗi ván — sigil hình bùa chú với dấu kích hoạt một lần.

```
/imagine prompt: a single small centered gold sigil overlay symbol, centered gold talisman paper charm with single activation flash mark, imperial gold (#D4AF37) dominant glow, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
