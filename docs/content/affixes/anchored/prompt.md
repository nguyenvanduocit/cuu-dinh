# Cố Định — MJ v6 prompt

**Format**: append `[STYLE SUFFIX]` từ `../../style/style-suffix.md`.

Linh khí không bị nhiễm khí sau va chạm — nét glyph hình mỏ neo cố định, dấu bình ổn.

```
/imagine prompt: a single small rune glyph overlay symbol, calm blue anchor stabilize motif with locked pin holding center steady, minimal, on transparent dark background, readable at tiny size, [STYLE SUFFIX] --ar 1:1 --stylize 80
```
